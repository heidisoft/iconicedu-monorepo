# Conventions

Full conventions: [docs/codebase/CONVENTIONS.md](../docs/codebase/CONVENTIONS.md).

- Naming, formatting/linting (Prettier + ESLint flat config), imports/aliases, error/logging, and testing conventions all live there.
- Run `pnpm format:check` and `pnpm lint:affected` before handoff.
- Known convention violations already tracked are listed at the bottom of that doc — check before assuming a pattern is the intended one.
