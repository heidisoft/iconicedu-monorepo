name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, closed]

# Cancel in-progress runs for the same branch/PR when a new push comes in
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  TURBO_TELEMETRY_DISABLED: 1

jobs:
  # ──────────────────────────────────────────────────────────────
  # Job 1: Code quality — format · lint · typecheck
  # PRs: affected packages only (fast feedback)
  # ──────────────────────────────────────────────────────────────
  quality:
    # Branch protection depends on this exact job name.
    # If you rename it, update .github/branch-protection/main.json and
    # scripts/github/apply-branch-protection.sh in the same change.
    name: Quality (format · lint · typecheck)
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Fetch enough history for turbo --affected to compare against the base
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Restore Turbo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-quality-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-quality-${{ runner.os }}-

      # Generate Prisma client from schema — required before API typecheck.
      # Does not connect to a real database; it only reads prisma/schema.prisma.
      - name: Generate Prisma client
        run: pnpm --filter api db:generate

      # Build shared packages so apps can resolve workspace imports.
      - name: Build shared packages
        run: pnpm build:packages

      # Always check all files — prettier is fast enough to run unconditionally.
      - name: Format check
        run: pnpm format:check

      # Temporarily disabled in GitHub Actions. Keep the local script in package.json
      # so the guard can be re-enabled here without reintroducing the implementation.
      # - name: Guard feature flags
      #   run: pnpm guard:feature-flags

      # PRs: only affected packages.
      - name: Lint + Typecheck
        run: pnpm exec turbo run lint typecheck ${{ github.event_name == 'pull_request' && '--affected' || '' }}
        env:
          TURBO_SCM_BASE: ${{ github.event.pull_request.base.sha }}
          TURBO_SCM_HEAD: ${{ github.sha }}

  # ──────────────────────────────────────────────────────────────
  # Job 2: Tests plus explicit coverage collection.
  # PRs: affected packages only. Push to main: all packages.
  # ──────────────────────────────────────────────────────────────
  test:
    # Branch protection depends on this exact job name.
    # If you rename it, update .github/branch-protection/main.json and
    # scripts/github/apply-branch-protection.sh in the same change.
    name: Test
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Restore Turbo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-test-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-test-${{ runner.os }}-

      - name: Build shared packages
        run: pnpm build:packages

      # Generate Prisma client — required for API test suite.
      - name: Generate Prisma client
        run: pnpm --filter api db:generate

      # PRs: only affected packages. Push to main: all packages.
      - name: Test
        run: pnpm exec turbo run test ${{ github.event_name == 'pull_request' && '--affected' || '' }}
        env:
          CI: true
          TURBO_SCM_BASE: ${{ github.event.pull_request.base.sha }}
          TURBO_SCM_HEAD: ${{ github.sha }}

      - name: Collect coverage
        run: pnpm exec turbo run test:coverage ${{ github.event_name == 'pull_request' && '--affected' || '' }}
        env:
          CI: true
          TURBO_SCM_BASE: ${{ github.event.pull_request.base.sha }}
          TURBO_SCM_HEAD: ${{ github.sha }}

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: |
            apps/web/coverage/
            apps/api/coverage/
            apps/mobile/coverage/
            packages/ui-web/coverage/
            packages/ui-native/coverage/
            packages/shared-types/coverage/
            packages/utils/coverage/
          if-no-files-found: ignore
          retention-days: 14

  # ──────────────────────────────────────────────────────────────
  # Job 3: Bundle sizes — builds all three apps on both the PR
  # branch and the base branch, measures raw + gzip sizes, and
  # uploads a JSON artifact consumed by preview-env.yml when it
  # assembles the unified "Preview Environment Ready" PR comment.
  # No PR comment is posted here. Informational only — not a gate.
  # ──────────────────────────────────────────────────────────────
  bundle-size:
    name: Bundle Size
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: pnpm

      # Write the measurement script once — reused for both PR and base builds.
      - name: Write measurement script
        run: |
          cat > /tmp/measure.js << 'EOF'
          const fs = require('fs'), path = require('path'), zlib = require('zlib');
          function walk(dir, exts) {
            if (!fs.existsSync(dir)) return [];
            const out = [];
            for (const e of fs.readdirSync(dir, { withFileTypes: true })) {
              const full = path.join(dir, e.name);
              if (e.isDirectory()) out.push(...walk(full, exts));
              else if (exts.some(x => e.name.endsWith(x)) && !e.name.endsWith('.map')) out.push(full);
            }
            return out;
          }
          function measure(dir, exts) {
            const files = walk(dir, exts);
            let raw = 0, gz = 0;
            const top = [];
            for (const f of files) {
              const buf = fs.readFileSync(f);
              const gzLen = zlib.gzipSync(buf).length;
              raw += buf.length; gz += gzLen;
              top.push({ name: path.basename(f), gz: gzLen });
            }
            top.sort((a, b) => b.gz - a.gz);
            return { raw, gz, count: files.length, top: top.slice(0, 5) };
          }
          const out = {
            web:    { js: measure('apps/web/.next/static', ['.js']), css: measure('apps/web/.next/static', ['.css']) },
            mobile: { js: measure('apps/mobile/.expo/dist/_expo/static/js', ['.js', '.hbc']) },
            api:    { js: measure('apps/api/dist', ['.js']) },
          };
          process.stdout.write(JSON.stringify(out));
          EOF

      # ── PR branch build ────────────────────────────────────────
      # Secrets can't use || fallback inside ${{}} — use bash :- via GITHUB_ENV.
      - name: Set build env vars
        run: |
          echo "NEXT_PUBLIC_SUPABASE_URL=${_URL:-https://placeholder.supabase.co}" >> "$GITHUB_ENV"
          echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=${_ANON:-placeholder-anon-key}" >> "$GITHUB_ENV"
          echo "SUPABASE_SERVICE_ROLE_KEY=${_SRK:-placeholder-service-role-key}" >> "$GITHUB_ENV"
          echo "EXPO_PUBLIC_SUPABASE_URL=${_URL:-https://placeholder.supabase.co}" >> "$GITHUB_ENV"
          echo "EXPO_PUBLIC_SUPABASE_ANON_KEY=${_ANON:-placeholder-anon-key}" >> "$GITHUB_ENV"
        env:
          _URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          _ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          _SRK: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Install dependencies (PR)
        run: pnpm install --frozen-lockfile

      - name: Build all apps (PR branch)
        run: |
          pnpm --filter api db:generate
          pnpm build:packages
          pnpm --filter web build
          (cd apps/mobile && npx expo export --platform android --output-dir .expo/dist)
          pnpm --filter api build
        env:
          EXPO_NO_TELEMETRY: 1
          TURBO_TELEMETRY_DISABLED: 1

      - name: Measure PR branch sizes
        run: node /tmp/measure.js > /tmp/pr-sizes.json

      # ── Base branch build ──────────────────────────────────────
      - name: Checkout base branch
        run: git checkout origin/${{ github.base_ref }}

      - name: Install dependencies (base)
        run: pnpm install --frozen-lockfile

      # continue-on-error: base may differ enough to fail a sub-build;
      # missing data renders as "—" in the comment rather than blocking.
      - name: Build all apps (base branch)
        continue-on-error: true
        run: |
          pnpm --filter api db:generate || true
          pnpm build:packages
          pnpm --filter web build
          (cd apps/mobile && npx expo export --platform android --output-dir .expo/dist) || true
          pnpm --filter api build || true
        env:
          EXPO_NO_TELEMETRY: 1
          TURBO_TELEMETRY_DISABLED: 1

      - name: Measure base branch sizes
        run: node /tmp/measure.js > /tmp/base-sizes.json

      - name: Combine into bundle-sizes artifact
        run: |
          node -e "
            const pr   = JSON.parse(require('fs').readFileSync('/tmp/pr-sizes.json', 'utf8'));
            const base = JSON.parse(require('fs').readFileSync('/tmp/base-sizes.json', 'utf8'));
            require('fs').writeFileSync('/tmp/bundle-sizes.json', JSON.stringify({ pr, base }));
          "

      - name: Upload bundle sizes artifact
        uses: actions/upload-artifact@v4
        with:
          name: bundle-sizes
          path: /tmp/bundle-sizes.json
          retention-days: 7

  # ──────────────────────────────────────────────────────────────
  # Job 4: Build apps — only runs after quality + test both pass
  # ──────────────────────────────────────────────────────────────
  build:
    # Branch protection depends on this exact job name.
    # If you rename it, update .github/branch-protection/main.json and
    # scripts/github/apply-branch-protection.sh in the same change.
    name: Build
    if: github.event.action != 'closed'
    needs: [quality, test]
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Restore Turbo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-build-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-build-${{ runner.os }}-

      - name: Generate Prisma client
        run: pnpm --filter api db:generate

      # Set build env vars — use repo secrets when available, fall back to
      # placeholder values so the Next.js build succeeds in forks / fresh repos.
      - name: Set build env vars
        run: |
          echo "NEXT_PUBLIC_SUPABASE_URL=${_URL:-https://placeholder.supabase.co}" >> "$GITHUB_ENV"
          echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=${_ANON:-placeholder-anon-key}" >> "$GITHUB_ENV"
          echo "SUPABASE_SERVICE_ROLE_KEY=${_SRK:-placeholder-service-role-key}" >> "$GITHUB_ENV"
        env:
          _URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          _ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          _SRK: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Build all
        run: pnpm build

  # ──────────────────────────────────────────────────────────────
  # Job 5: Verify Preview Environment Status
  # Runs after build + bundle-size both complete. Creates a
  # Supabase preview branch, deploys Railway API, configures
  # Vercel env vars, and posts a unified PR comment.
  # ──────────────────────────────────────────────────────────────
  preview-environment:
    name: Verify Preview Environment Status
    if: github.event.action != 'closed'
    needs: [build, bundle-size]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      pull-requests: write
      deployments: write

    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      BRANCH_NAME: ${{ github.head_ref }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      # bundle-sizes is produced by the bundle-size job in this same run —
      # no run-id needed; download-artifact defaults to the current workflow run.
      - name: Download bundle sizes artifact
        id: bundle-artifact
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: bundle-sizes
          path: /tmp/artifacts

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      # 1. Create Supabase preview branch (idempotent — skips if already exists)
      - name: Create Supabase branch
        id: supabase-branch
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          # Use the GitHub branch name — stable across pushes to the same PR branch.
          SUPABASE_BRANCH="${BRANCH_NAME}"

          EXISTING_ID=$(supabase branches list \
            --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}" \
            --output json \
            | jq -r --arg name "$SUPABASE_BRANCH" '.[] | select(.name==$name) | .id // ""')

          if [[ -n "$EXISTING_ID" ]]; then
            echo "Branch $SUPABASE_BRANCH already exists — skipping create and seed"
            echo "branch_new=false" >> "$GITHUB_OUTPUT"
          else
            echo "Creating branch: $SUPABASE_BRANCH"
            supabase branches create "$SUPABASE_BRANCH" \
              --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}" \
              --region ap-southeast-1
            echo "branch_new=true" >> "$GITHUB_OUTPUT"
          fi

          echo "Waiting for branch migrations to complete..."
          for i in $(seq 1 30); do
            BRANCH_DATA=$(supabase branches list \
              --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}" \
              --output json \
              | jq --arg name "$SUPABASE_BRANCH" '.[] | select(.name==$name)')

            STATUS=$(echo "$BRANCH_DATA" | jq -r '.status // ""')
            BRANCH_ID=$(echo "$BRANCH_DATA" | jq -r '.id // ""')

            echo "  Status: ${STATUS:-(pending)}"

            if [[ "$STATUS" == "MIGRATIONS_PASSED" || "$STATUS" == "FUNCTIONS_DEPLOYED" ]]; then
              echo "branch_id=$BRANCH_ID" >> "$GITHUB_OUTPUT"
              BRANCH_REF=$(echo "$BRANCH_DATA" | jq -r '.project_ref // .ref // ""')
              echo "branch_ref=$BRANCH_REF" >> "$GITHUB_OUTPUT"
              break
            elif [[ "$STATUS" == "MIGRATIONS_FAILED" ]]; then
              echo "::error::Supabase branch migrations failed"
              exit 1
            fi

            if [[ $i -eq 30 ]]; then
              echo "::error::Timed out waiting for Supabase branch"
              exit 1
            fi
            sleep 10
          done

      # 2. Fetch branch credentials
      - name: Fetch branch credentials
        id: branch-creds
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          BRANCH_REF="${{ steps.supabase-branch.outputs.branch_ref }}"

          if [[ -z "$BRANCH_REF" || "$BRANCH_REF" == "null" ]]; then
            echo "::error::branch_ref is empty — branches list did not return a project_ref for this branch"
            exit 1
          fi
          echo "Branch project ref: $BRANCH_REF"

          ANON_KEY=$(curl -sf \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            "https://api.supabase.com/v1/projects/${BRANCH_REF}/api-keys" \
            | jq -r '.[] | select(.name=="anon") | .api_key')
          echo "::add-mask::$ANON_KEY"
          echo "anon_key=$ANON_KEY" >> "$GITHUB_OUTPUT"

          SERVICE_ROLE_KEY=$(curl -sf \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            "https://api.supabase.com/v1/projects/${BRANCH_REF}/api-keys" \
            | jq -r '.[] | select(.name=="service_role") | .api_key')
          echo "::add-mask::$SERVICE_ROLE_KEY"
          echo "service_role_key=$SERVICE_ROLE_KEY" >> "$GITHUB_OUTPUT"

          REGION="ap-southeast-1"
          DB_PASS="${{ secrets.SUPABASE_DB_PASSWORD }}"
          echo "::add-mask::$DB_PASS"
          DATABASE_URL="postgresql://postgres.${BRANCH_REF}:${DB_PASS}@aws-0-${REGION}.pooler.supabase.com:6543/postgres"
          DIRECT_URL="postgresql://postgres:${DB_PASS}@db.${BRANCH_REF}.supabase.co:5432/postgres"
          echo "::add-mask::$DATABASE_URL"
          echo "::add-mask::$DIRECT_URL"
          echo "database_url=$DATABASE_URL" >> "$GITHUB_OUTPUT"
          echo "direct_url=$DIRECT_URL" >> "$GITHUB_OUTPUT"

          JWT_SECRET=$(curl -sf \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            "https://api.supabase.com/v1/projects/${BRANCH_REF}/config/auth" \
            | jq -r '.jwt_secret')
          echo "::add-mask::$JWT_SECRET"
          echo "jwt_secret=$JWT_SECRET" >> "$GITHUB_OUTPUT"

          echo "branch_api_url=https://${BRANCH_REF}.supabase.co" >> "$GITHUB_OUTPUT"

      # 3. Deploy Railway preview environment
      - name: Deploy Railway preview API
        id: railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_API_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          set -euo pipefail

          if [[ -z "${PR_NUMBER:-}" ]]; then
            echo "::error::PR_NUMBER is empty; cannot derive Railway preview environment name"
            exit 1
          fi
          ENV_NAME="preview-${PR_NUMBER}"
          SERVICE_ID="${{ secrets.RAILWAY_SERVICE_ID }}"
          GQL="https://backboard.railway.app/graphql/v2"

          api() {
            curl -sf \
              -H "Authorization: Bearer $RAILWAY_TOKEN" \
              -H "Content-Type: application/json" \
              "$GQL" \
              -d "$1" || echo "{}"
          }

          echo "Ensuring Railway environment: $ENV_NAME"
          CREATE_RESP=$(api "{\"query\":\"mutation { environmentCreate(input: {projectId: \\\"$RAILWAY_PROJECT_ID\\\", name: \\\"$ENV_NAME\\\"}) { id } }\"}")

          ENV_ID=$(echo "$CREATE_RESP" | jq -r '.data.environmentCreate.id // empty')
          echo "Create response: $(echo "$CREATE_RESP" | jq '.errors // "ok"')"

          if [[ -z "$ENV_ID" ]]; then
            echo "Environment may already exist — fetching by name"
            LIST_RESP=$(api "{\"query\":\"query { project(id: \\\"$RAILWAY_PROJECT_ID\\\") { environments { edges { node { id name } } } } }\"}")
            ENV_ID=$(echo "$LIST_RESP" | jq -r ".data.project.environments.edges[]? | select(.node.name==\"$ENV_NAME\") | .node.id // empty")
          fi

          if [[ -z "$ENV_ID" ]]; then
            echo "::error::Failed to create or find Railway environment $ENV_NAME"
            exit 1
          fi
          echo "Environment ID: $ENV_ID"

          VARS_JSON=$(jq -n \
            --arg db    "${{ steps.branch-creds.outputs.database_url }}" \
            --arg dir   "${{ steps.branch-creds.outputs.direct_url }}" \
            --arg surl  "${{ steps.branch-creds.outputs.branch_api_url }}" \
            --arg srkey "${{ steps.branch-creds.outputs.service_role_key }}" \
            --arg jwt   "${{ steps.branch-creds.outputs.jwt_secret }}" \
            --arg rem   "${{ secrets.INTERNAL_REMINDERS_TOKEN }}" \
            --arg not   "${{ secrets.INTERNAL_NOTIFICATIONS_TOKEN }}" \
            --arg phk   "${{ secrets.POSTHOG_KEY }}" \
            --arg phh   "${{ secrets.NEXT_PUBLIC_POSTHOG_HOST }}" \
            '{DATABASE_URL:$db, DIRECT_URL:$dir, SUPABASE_URL:$surl,
              SUPABASE_SERVICE_ROLE_KEY:$srkey, JWT_SECRET:$jwt,
              INTERNAL_REMINDERS_TOKEN_API:$rem, INTERNAL_NOTIFICATIONS_TOKEN:$not,
              NODE_ENV:"production", NEXT_PUBLIC_APP_ENV:"preview",
              POSTHOG_KEY:$phk, POSTHOG_HOST:$phh}')

          api "$(jq -n \
            --arg pid "$RAILWAY_PROJECT_ID" \
            --arg eid "$ENV_ID" \
            --arg sid "$SERVICE_ID" \
            --argjson vars "$VARS_JSON" \
            '{query:"mutation variableCollectionUpsert($input: VariableCollectionUpsertInput!) { variableCollectionUpsert(input: $input) }",
              variables:{input:{projectId:$pid, environmentId:$eid, serviceId:$sid, variables:$vars}}}')"
          echo "Variables set"

          echo "Triggering Railway deployment..."
          DEPLOY_RESP=$(api "{\"query\":\"mutation { serviceInstanceDeploy(serviceId: \\\"$SERVICE_ID\\\", environmentId: \\\"$ENV_ID\\\") }\"}")
          echo "Deploy response: $(echo "$DEPLOY_RESP" | jq '.errors // .data')"

          service_instance_info() {
            api "{\"query\":\"query { serviceInstance(serviceId: \\\"$SERVICE_ID\\\", environmentId: \\\"$ENV_ID\\\") { latestDeployment { status } domains { serviceDomains { id domain targetPort } } } }\"}"
          }

          get_service_domain() {
            local info
            info=$(service_instance_info)
            echo "$info" | jq -r '.data.serviceInstance.domains.serviceDomains[]? | select((.targetPort // 3000) == 3000) | .domain' | head -n 1
          }

          echo "Waiting for Railway deployment to stabilize..."
          DEPLOY_STATUS=""
          for i in $(seq 1 12); do
            DEPLOY_INFO=$(service_instance_info)
            DEPLOY_STATUS=$(echo "$DEPLOY_INFO" | jq -r '.data.serviceInstance.latestDeployment.status // empty')
            EXISTING_DOMAIN=$(echo "$DEPLOY_INFO" | jq -r '.data.serviceInstance.domains.serviceDomains[]? | select((.targetPort // 3000) == 3000) | .domain' | head -n 1)
            echo "  Status: ${DEPLOY_STATUS:-(pending)}, Domain: ${EXISTING_DOMAIN:-(none)} (attempt $i)"

            case "$DEPLOY_STATUS" in
              SUCCESS|FAILED|CRASHED|REMOVED) break ;;
            esac

            sleep 10
          done

          if [[ "$DEPLOY_STATUS" == "FAILED" || "$DEPLOY_STATUS" == "CRASHED" ]]; then
            echo "::warning::Railway deployment ended with status $DEPLOY_STATUS"
          fi

          RAILWAY_DOMAIN="$(get_service_domain)"
          if [[ -n "$RAILWAY_DOMAIN" ]]; then
            echo "Using existing Railway domain: $RAILWAY_DOMAIN"
          else
            echo "No Railway service domain found for port 3000 — creating one"
            DOMAIN_CREATE_RESP=$(api "$(jq -n \
              --arg eid "$ENV_ID" \
              --arg sid "$SERVICE_ID" \
              '{query:"mutation serviceDomainCreate($input: ServiceDomainCreateInput!) { serviceDomainCreate(input: $input) { id domain targetPort } }", variables:{input:{environmentId:$eid, serviceId:$sid, targetPort:3000}}}')")
            RAILWAY_DOMAIN=$(echo "$DOMAIN_CREATE_RESP" | jq -r '.data.serviceDomainCreate.domain // empty')
            if [[ -z "$RAILWAY_DOMAIN" ]]; then
              RAILWAY_DOMAIN="$(get_service_domain)"
            fi
          fi

          if [[ -z "$RAILWAY_DOMAIN" ]]; then
            for i in $(seq 1 12); do
              RAILWAY_DOMAIN="$(get_service_domain)"
              echo "  Domain lookup attempt $i: ${RAILWAY_DOMAIN:-(none)}"
              if [[ -n "$RAILWAY_DOMAIN" ]]; then break; fi
              sleep 10
            done
          fi

          if [[ -n "$RAILWAY_DOMAIN" ]]; then
            HEALTHY_URL=""
            for i in $(seq 1 12); do
              HEALTH_STATUS=$(curl -sf -o /dev/null -w "%{http_code}" \
                "https://${RAILWAY_DOMAIN}/healthz" 2>/dev/null || echo "000")
              echo "  Health check attempt $i: $HEALTH_STATUS"
              if [[ "$HEALTH_STATUS" == "200" ]]; then
                HEALTHY_URL="https://${RAILWAY_DOMAIN}"
                break
              fi
              sleep 10
            done
            if [[ -n "$HEALTHY_URL" ]]; then
              echo "railway_api_url=${HEALTHY_URL}" >> "$GITHUB_OUTPUT"
            else
              echo "::warning::Railway domain exists but /healthz did not return 200 within the retry window"
              echo "railway_api_url=https://${RAILWAY_DOMAIN}" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "::warning::Railway domain was not available after the retry window"
            echo "railway_api_url=pending" >> "$GITHUB_OUTPUT"
          fi

      # 4. Configure Vercel preview env vars
      - name: Configure Vercel preview env vars
        id: vercel
        continue-on-error: true
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          set -euo pipefail
          BRANCH="${{ env.BRANCH_NAME }}"
          PROJECT_ID="${VERCEL_PROJECT_ID:-}"
          ORG_ID="${VERCEL_ORG_ID:-}"
          TEAM_AMP=""
          TEAM_Q=""

          fail_vercel_config() { echo "::error::$1"; exit 1; }

          [[ -z "$PROJECT_ID" ]] && fail_vercel_config "Missing VERCEL_PROJECT_ID."
          [[ -z "$ORG_ID" ]] && fail_vercel_config "Missing VERCEL_ORG_ID."

          case "$ORG_ID" in
            team_*) TEAM_AMP="&teamId=${ORG_ID}"; TEAM_Q="?teamId=${ORG_ID}" ;;
            user_*) ;;
            *) fail_vercel_config "VERCEL_ORG_ID must start with team_ or user_." ;;
          esac

          PROJECT_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $VERCEL_TOKEN" \
            "https://api.vercel.com/v9/projects/${PROJECT_ID}${TEAM_Q}")
          if [[ "$PROJECT_STATUS" != "200" ]]; then
            fail_vercel_config "Vercel project preflight failed (HTTP ${PROJECT_STATUS})."
          fi

          set_vercel_env() {
            local KEY="$1" VALUE="$2"
            EXISTING_ID=$(curl -sf \
              -H "Authorization: Bearer $VERCEL_TOKEN" \
              "https://api.vercel.com/v10/projects/${PROJECT_ID}/env?gitBranch=${BRANCH}${TEAM_AMP}" \
              | jq -r --arg k "$KEY" '.envs[] | select(.key==$k) | .id' 2>/dev/null || echo "")
            if [[ -n "$EXISTING_ID" ]]; then
              curl -sf -X DELETE \
                -H "Authorization: Bearer $VERCEL_TOKEN" \
                "https://api.vercel.com/v10/projects/${PROJECT_ID}/env/${EXISTING_ID}${TEAM_Q}" > /dev/null
            fi
            POST_STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
              -H "Authorization: Bearer $VERCEL_TOKEN" \
              -H "Content-Type: application/json" \
              "https://api.vercel.com/v10/projects/${PROJECT_ID}/env${TEAM_Q}" \
              -d "$(jq -n --arg k "$KEY" --arg v "$VALUE" --arg b "$BRANCH" \
                '{"key":$k,"value":$v,"target":["preview"],"gitBranch":$b,"type":"encrypted"}')")
            if [[ ! "$POST_STATUS" =~ ^2[0-9][0-9]$ ]]; then
              echo "::error::Vercel env write failed for $KEY (HTTP $POST_STATUS)"
              exit 1
            fi
          }

          set_vercel_env "NEXT_PUBLIC_API_URL" "${{ steps.railway.outputs.railway_api_url }}"
          set_vercel_env "NEXT_PUBLIC_APP_ENV" "preview"

          LATEST_DEPLOYMENT=$(curl -sf \
            -H "Authorization: Bearer $VERCEL_TOKEN" \
            "https://api.vercel.com/v6/deployments?projectId=${PROJECT_ID}&target=preview&limit=1&meta-githubCommitRef=${BRANCH}${TEAM_AMP}" \
            | jq -r '.deployments[0].url // ""')

          if [[ -n "$LATEST_DEPLOYMENT" ]]; then
            echo "vercel_url=https://${LATEST_DEPLOYMENT}" >> "$GITHUB_OUTPUT"
          else
            echo "vercel_url=https://vercel.com/dashboard" >> "$GITHUB_OUTPUT"
          fi

      # 6a. Run Lighthouse against the PR preview deployment
      - name: Run Lighthouse CI (PR)
        id: lighthouse-pr
        # Only run when Vercel produced a real preview URL.
        if: >-
          steps.vercel.outputs.vercel_url != '' &&
          steps.vercel.outputs.vercel_url != 'https://vercel.com/dashboard'
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: ${{ steps.vercel.outputs.vercel_url }}
          configPath: apps/web/lighthouserc.js
          uploadArtifacts: true
          artifactName: lighthouse-results-pr
          temporaryPublicStorage: true

      # 6b. Run Lighthouse against production (main) for drift comparison.
      # Skipped when LHCI_PRODUCTION_URL repo variable is not set.
      - name: Run Lighthouse CI (main / production)
        id: lighthouse-main
        if: vars.LHCI_PRODUCTION_URL != ''
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: ${{ vars.LHCI_PRODUCTION_URL }}
          configPath: apps/web/lighthouserc.js
          uploadArtifacts: false
          temporaryPublicStorage: false

      # 7. Post / update the single unified PR comment
      - name: Post preview environment comment
        uses: actions/github-script@v7
        env:
          LH_PR_MANIFEST: ${{ steps.lighthouse-pr.outputs.manifest }}
          LH_PR_LINKS: ${{ steps.lighthouse-pr.outputs.links }}
          LH_MAIN_MANIFEST: ${{ steps.lighthouse-main.outputs.manifest }}
        with:
          script: |
            const fs = require('fs');
            const prNumber  = context.payload.pull_request.number;
            const webUrl    = '${{ steps.vercel.outputs.vercel_url }}';
            const apiUrl    = '${{ steps.railway.outputs.railway_api_url }}';
            const branchRef = '${{ steps.supabase-branch.outputs.branch_ref }}';
            const studioUrl = `https://supabase.com/dashboard/project/${branchRef}`;

            // ── Helpers ────────────────────────────────────────────────────────
            const fmt = (bytes) => {
              if (bytes == null) return '—';
              if (bytes < 1024) return `${bytes} B`;
              if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} kB`;
              return `${(bytes / 1024 / 1024).toFixed(2)} MB`;
            };
            const sizeDelta = (pr, base) => {
              if (pr == null || base == null) return '';
              const d = pr - base;
              if (d === 0) return ' *(no change)*';
              return ` *(${d > 0 ? '+' : ''}${fmt(d)})*`;
            };
            const scoreIcon = (v) => {
              if (v == null) return '—';
              const n = Math.round(v * 100);
              if (n >= 90) return `${n} ✅`;
              if (n >= 50) return `${n} ⚠️`;
              return `${n} ❌`;
            };
            const scoreDelta = (pr, base) => {
              if (pr == null || base == null) return '—';
              const d = Math.round((pr - base) * 100);
              if (d === 0) return '=';
              return d > 0 ? `+${d}` : `${d}`;
            };

            // ── Bundle sizes ───────────────────────────────────────────────────
            let bundleSection = '\n\n> ⚠️ Bundle sizes unavailable — the bundle-size job may have timed out or failed.';
            const bundlePath = '/tmp/artifacts/bundle-sizes.json';
            if (fs.existsSync(bundlePath)) {
              try {
                const { pr, base } = JSON.parse(fs.readFileSync(bundlePath, 'utf8'));
                const rows = [
                  ['Web JS',    pr.web?.js?.gz,    base.web?.js?.gz,    pr.web?.js?.count],
                  ['Web CSS',   pr.web?.css?.gz,   base.web?.css?.gz,   pr.web?.css?.count],
                  ['Mobile JS', pr.mobile?.js?.gz, base.mobile?.js?.gz, pr.mobile?.js?.count],
                  ['API JS',    pr.api?.js?.gz,    base.api?.js?.gz,    pr.api?.js?.count],
                ];
                bundleSection = [
                  '',
                  '### Bundle Sizes (gzip)',
                  '',
                  '| App | This PR | Base branch | Files |',
                  '|-----|---------|-------------|-------|',
                  ...rows.map(([label, prGz, baseGz, count]) =>
                    `| ${label} | ${fmt(prGz)}${sizeDelta(prGz, baseGz)} | ${fmt(baseGz)} | ${count ?? '—'} |`
                  ),
                ].join('\n');
              } catch (e) {
                bundleSection = `\n\n> ⚠️ Bundle size data could not be parsed: ${e.message}`;
              }
            }

            // ── Lighthouse ─────────────────────────────────────────────────────
            let lighthouseSection = '';
            const prManifestRaw   = process.env.LH_PR_MANIFEST   || '';
            const prLinksRaw      = process.env.LH_PR_LINKS       || '';
            const mainManifestRaw = process.env.LH_MAIN_MANIFEST  || '';
            if (prManifestRaw) {
              try {
                const prManifest   = JSON.parse(prManifestRaw);
                const links        = prLinksRaw ? JSON.parse(prLinksRaw) : {};
                const prResult     = prManifest.find(r => r.isRepresentativeRun) || prManifest[0];
                const s            = prResult.summary;

                let base = null;
                if (mainManifestRaw) {
                  try {
                    const mainManifest = JSON.parse(mainManifestRaw);
                    const mainResult   = mainManifest.find(r => r.isRepresentativeRun) || mainManifest[0];
                    base = mainResult.summary;
                  } catch {}
                }

                const reportUrl  = links[prResult.url] || '';
                const reportLink = reportUrl ? ` ([full report](${reportUrl}))` : '';
                const baseNote   = base ? '' : ' *(add `LHCI_PRODUCTION_URL` repo variable to enable vs main)*';

                lighthouseSection = [
                  '',
                  `### Lighthouse (vs \`main\`)${reportLink}${baseNote}`,
                  '',
                  '| Category | Score | vs main |',
                  '|----------|:-----:|:-------:|',
                  `| Performance    | ${scoreIcon(s.performance)}    | ${scoreDelta(s.performance,    base?.performance)} |`,
                  `| Accessibility  | ${scoreIcon(s.accessibility)}  | ${scoreDelta(s.accessibility,  base?.accessibility)} |`,
                  `| Best Practices | ${scoreIcon(s['best-practices'])} | ${scoreDelta(s['best-practices'], base?.['best-practices'])} |`,
                  `| SEO            | ${scoreIcon(s.seo)}            | ${scoreDelta(s.seo,            base?.seo)} |`,
                ].join('\n');
              } catch (e) {
                lighthouseSection = `\n\n> ⚠️ Lighthouse results could not be parsed: ${e.message}`;
              }
            }

            // ── Assemble comment ───────────────────────────────────────────────
            const body = [
              '## Preview Environment Ready',
              '',
              '| Service | URL |',
              '|---------|-----|',
              `| Web (Vercel) | ${webUrl} |`,
              `| API (Railway) | ${apiUrl}/healthz |`,
              `| Supabase Studio | ${studioUrl} |`,
              '',
              `**Mobile build** — [Actions → EAS Preview Build](../../actions/workflows/eas-preview-build.yml) → PR number: \`${prNumber}\``,
              bundleSection,
              lighthouseSection,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });

            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('Preview Environment Ready')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body,
              });
            }

  # ──────────────────────────────────────────────────────────────
  # Job 6: Cleanup — runs only when a PR is closed (merged or not)
  # Deletes the Supabase preview branch, Railway environment, and
  # Vercel branch-scoped env vars for this PR.
  # ──────────────────────────────────────────────────────────────
  cleanup:
    name: Clean Up Preview Environemnts
    if: github.event.action == 'closed'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: write

    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      BRANCH_NAME: ${{ github.head_ref }}

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      # ── 1. Delete Supabase preview branch ──────────────────────
      - name: Delete Supabase branch
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          BRANCH_ID=$(supabase branches list \
            --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}" \
            --output json \
            | jq -r --arg name "$BRANCH_NAME" '.[] | select(.name==$name) | .id // ""')

          if [[ -n "$BRANCH_ID" ]]; then
            supabase branches delete "$BRANCH_ID" \
              --project-ref "${{ secrets.SUPABASE_PROJECT_ID }}"
            echo "Deleted Supabase branch: $BRANCH_NAME ($BRANCH_ID)"
          else
            echo "No Supabase branch found for $BRANCH_NAME — skipping"
          fi

      # ── 2. Delete Railway preview environment ──────────────────
      - name: Delete Railway environment
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_API_TOKEN }}
        run: |
          if [[ -z "${PR_NUMBER:-}" ]]; then
            echo "::error::PR_NUMBER is empty; cannot derive Railway preview environment name"
            exit 1
          fi
          ENV_NAME="preview-${PR_NUMBER}"
          GQL="https://backboard.railway.app/graphql/v2"

          ENV_ID=$(curl -sf \
            -H "Authorization: Bearer $RAILWAY_TOKEN" \
            -H "Content-Type: application/json" "$GQL" \
            -d "{\"query\":\"query { project(id: \\\"${{ secrets.RAILWAY_PROJECT_ID }}\\\") { environments { edges { node { id name } } } } }\"}" \
            | jq -r ".data.project.environments.edges[]? | select(.node.name==\"$ENV_NAME\") | .node.id // empty")

          if [[ -n "$ENV_ID" ]]; then
            curl -sf \
              -H "Authorization: Bearer $RAILWAY_TOKEN" \
              -H "Content-Type: application/json" "$GQL" \
              -d "{\"query\":\"mutation { environmentDelete(id: \\\"$ENV_ID\\\") }\"}"
            echo "Deleted Railway environment: $ENV_NAME ($ENV_ID)"
          else
            echo "No Railway environment found for $ENV_NAME — skipping"
          fi

      # ── 3. Remove Vercel branch-scoped env vars ─────────────────
      - name: Remove Vercel branch env vars
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          set -euo pipefail
          BRANCH="${{ env.BRANCH_NAME }}"
          PROJECT_ID="${VERCEL_PROJECT_ID:-}"
          ORG_ID="${VERCEL_ORG_ID:-}"
          TEAM_AMP=""
          TEAM_Q=""

          fail_vercel_config() { echo "::error::$1"; exit 1; }

          [[ -z "$PROJECT_ID" ]] && fail_vercel_config "Missing VERCEL_PROJECT_ID."
          [[ -z "$ORG_ID" ]] && fail_vercel_config "Missing VERCEL_ORG_ID."

          case "$ORG_ID" in
            team_*) TEAM_AMP="&teamId=${ORG_ID}"; TEAM_Q="?teamId=${ORG_ID}" ;;
            user_*) ;;
            *) fail_vercel_config "VERCEL_ORG_ID must start with team_ or user_." ;;
          esac

          PROJECT_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $VERCEL_TOKEN" \
            "https://api.vercel.com/v9/projects/${PROJECT_ID}${TEAM_Q}")
          if [[ "$PROJECT_STATUS" != "200" ]]; then
            fail_vercel_config "Vercel project preflight failed (HTTP ${PROJECT_STATUS})."
          fi

          ENV_IDS=$(curl -sf \
            -H "Authorization: Bearer $VERCEL_TOKEN" \
            "https://api.vercel.com/v10/projects/${PROJECT_ID}/env?gitBranch=${BRANCH}${TEAM_AMP}" \
            | jq -r '.envs[].id' 2>/dev/null || echo "")

          if [[ -n "$ENV_IDS" ]]; then
            while IFS= read -r ENV_ID; do
              curl -sf -X DELETE \
                -H "Authorization: Bearer $VERCEL_TOKEN" \
                "https://api.vercel.com/v10/projects/${PROJECT_ID}/env/${ENV_ID}${TEAM_Q}" > /dev/null \
                && echo "Deleted Vercel env var: $ENV_ID"
            done <<< "$ENV_IDS"
          else
            echo "No Vercel branch env vars found for $BRANCH — skipping"
          fi

      # ── 4. Post cleanup comment ─────────────────────────────────
      - name: Post cleanup comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: '🧹 Preview environment cleaned up (Supabase branch, Railway environment, and Vercel env vars removed).',
            });
