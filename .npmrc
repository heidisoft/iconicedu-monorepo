## pnpm + Expo/React Native compatibility
## node-linker=hoisted uses a flat node_modules structure (like npm/yarn).
## Required because jest-expo and react-native resolve transitive deps
## at runtime, which doesn't work with pnpm's default symlinked structure.
node-linker=hoisted
