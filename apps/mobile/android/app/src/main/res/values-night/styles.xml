<resources>
  <!--
    In dark mode, status bar icons should be LIGHT (white) so they are visible
    on dark backgrounds. This mirrors the light-mode styles.xml but inverts
    windowLightStatusBar. SystemBars will also set this dynamically at runtime.
  -->
  <style name="AppTheme" parent="Theme.EdgeToEdge">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:windowLightStatusBar">false</item>
  </style>
</resources>
