import type { ViewStyle } from 'react-native';

type RoleAvatarBadgeProps = {
  role?: string | null;
  size?: number;
  style?: ViewStyle;
};

export function RoleAvatarBadge({
  role: _role,
  size: _size = 22,
  style: _style,
}: RoleAvatarBadgeProps) {
  return null;
}
