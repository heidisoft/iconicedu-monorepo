// PostHog browser init lives in PostHogAnalytics (components/posthog-analytics.tsx).
// This file requires experimental.clientInstrumentationHook: true in next.config
// to actually execute — without that flag Next.js silently skips it.
// Keeping it as a no-op placeholder; safe to delete if not needed.
export function register() {}
