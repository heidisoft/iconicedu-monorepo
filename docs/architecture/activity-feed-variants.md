# Activity Feed Variants

This reference lists the activity event variants we currently create and how
they render into feed items.

Canonical rendering is owned by
`apps/api/src/lib/activity-feed/definitions/activity-definitions.ts`. Web and
mobile should consume API-projected feed items rather than maintaining local
activity definition mirrors.

## Shared Render Rules

| Field                       | Rule                                                                                                                                                       |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `secondary` / context label | Message rows prefer the per-recipient role-aware class label from `activityContext`; session rows show role context without repeating the class title.     |
| Parent session context      | `For {viewerStudentNames or studentNames} with {teacherNames}`. Example: `For Rhea + 1 with Ms. Denise`.                                                   |
| Teacher session context     | `With {studentNames}`. Example: `With Rhea + 3 students`.                                                                                                  |
| Student session context     | `With {teacherNames}`. Example: `With Ms. Denise + 1`.                                                                                                     |
| Staff session context       | `Parent: {guardianNames} · Student: {studentNames} · Teacher: {teacherNames}`.                                                                             |
| Source href                 | Explicit `payload.href`; otherwise `/{orgSlug}/dm/{channelId}`, `/{orgSlug}/c/{channelId}`, or `/{orgSlug}/s/{channelId}` based on route kind/class scope. |
| Feed preview                | `summary` renders as the preview card. `expandedContent` renders behind read-more behavior.                                                                |
| Icons                       | Feed rows use `content.leading.iconKey` when present, then fall back from `verb`. Web and mobile map icon keys to Lucide icons.                            |

## Activity Table

| Variation                         | Event type                      | Projected verb                  | Tab       | Importance  | Icon key / tone               | Headline primary                                                 | Headline secondary                                                         | Summary / preview                                                             | Expanded content                              | Action button               | Default notification    |
| --------------------------------- | ------------------------------- | ------------------------------- | --------- | ----------- | ----------------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | --------------------------------------------- | --------------------------- | ----------------------- |
| Channel or class text message     | `message.posted`                | `message.posted`                | `all`     | `normal`    | `MessageSquareDot` / `info`   | `{senderName}`                                                   | `sent a message in {contextLabel}`                                         | `payload.messagePreview` or `payload.content`                                 | Full message content                          | `Open message` / `outline`  | Push, immediate         |
| Direct message                    | `message.posted`                | `message.posted`                | `all`     | `normal`    | `MessagesSquare` / `info`     | `{senderName}`                                                   | `sent you a direct message`                                                | `payload.messagePreview` or `payload.content`                                 | Full message content                          | `Reply` / `outline`         | Push, immediate         |
| Mention in channel/class          | `message.mentioned`             | `message.mentioned`             | `all`     | `important` | `AtSign` / `info`             | `{senderName}`                                                   | `mentioned you in {contextLabel}`                                          | `payload.messagePreview` or `payload.content`                                 | Full message content                          | `View mention` / `outline`  | Push, standard          |
| Mention in direct message         | `message.mentioned`             | `message.mentioned`             | `all`     | `important` | `AtSign` / `info`             | `{senderName}`                                                   | `mentioned you in a direct message`                                        | `payload.messagePreview` or `payload.content`                                 | Full message content                          | `Reply` / `outline`         | Push, standard          |
| Thread reply in channel/class     | `message.thread_reply.posted`   | `message.thread_reply.posted`   | `all`     | `normal`    | `MessageSquareReply` / `info` | `{senderName}`                                                   | `replied in a thread in {contextLabel}`                                    | `payload.messagePreview` or `payload.content`                                 | Thread reply content                          | `Open thread` / `outline`   | Push, standard          |
| Thread reply in direct message    | `message.thread_reply.posted`   | `message.thread_reply.posted`   | `all`     | `normal`    | `MessageSquareReply` / `info` | `{senderName}`                                                   | `replied in your direct message thread`                                    | `payload.messagePreview` or `payload.content`                                 | Thread reply content                          | `Open thread` / `outline`   | Push, standard          |
| File upload in channel/class      | `file.uploaded`                 | `file.uploaded`                 | `all`     | `normal`    | `FileBadge` / `info`          | `{senderName}`                                                   | `shared a file in {contextLabel}`                                          | `payload.fileName` or `payload.name`                                          | File details and optional message             | `Open file` / `outline`     | Push, standard          |
| File upload in direct message     | `file.uploaded`                 | `file.uploaded`                 | `all`     | `normal`    | `FileBadge` / `info`          | `{senderName}`                                                   | `shared a file with you`                                                   | `payload.fileName` or `payload.name`                                          | File details and optional message             | `Open file` / `outline`     | Push, standard          |
| Image upload in channel/class     | `image.uploaded`                | `image.uploaded`                | `all`     | `normal`    | `BookImage` / `info`          | `{senderName}`                                                   | `shared an image in {contextLabel}`                                        | `payload.fileName` or `payload.name`                                          | Image preview details and optional message    | `View image` / `outline`    | Push, standard          |
| Image upload in direct message    | `image.uploaded`                | `image.uploaded`                | `all`     | `normal`    | `BookImage` / `info`          | `{senderName}`                                                   | `shared an image with you`                                                 | `payload.fileName` or `payload.name`                                          | Image preview details and optional message    | `View image` / `outline`    | Push, standard          |
| Voice/audio upload in channel     | `audio.uploaded`                | `audio.uploaded`                | `all`     | `normal`    | `FileHeadphone` / `info`      | `{senderName}`                                                   | `sent a voice message in {contextLabel}`                                   | `payload.fileName` or `payload.name`                                          | Audio details and optional message            | `Play audio` / `outline`    | Push, standard          |
| Voice/audio upload in DM          | `audio.uploaded`                | `audio.uploaded`                | `all`     | `normal`    | `FileHeadphone` / `info`      | `{senderName}`                                                   | `sent you a voice message`                                                 | `payload.fileName` or `payload.name`                                          | Audio details and optional message            | `Play audio` / `outline`    | Push, standard          |
| Message reaction in class/channel | `reaction.added`                | `reaction.added`                | `all`     | `normal`    | `SmilePlus` / `info`          | `{senderName} reacted {emoji} to your message in {contextLabel}` | None                                                                       | `Your message: {messagePreview}`                                              | Original message with reaction context        | `View message` / `outline`  | Push, quiet             |
| Direct-message reaction           | `reaction.added`                | `reaction.added`                | `all`     | `normal`    | `MessageSquare` / `info`      | `{senderName} reacted {emoji} to your direct message`            | None                                                                       | `Your message: {messagePreview}`                                              | Original message with reaction context        | `View message` / `outline`  | Push, quiet             |
| Single class session rescheduled  | `class.session.rescheduled`     | `class.session.rescheduled`     | `classes` | `important` | `CalendarCheck` / `info`      | `{classTitle}`                                                   | `session was rescheduled · {roleContext} · New time: {newSessionDateTime}` | `{classTitle} session {oldSessionDateTime} was moved to {newSessionDateTime}` | `Reason: {rescheduledReason}`                 | `Open class` / `outline`    | Push + email, immediate |
| Single class session canceled     | `class.session.canceled`        | `class.session.canceled`        | `classes` | `important` | `CalendarX` / `warning`       | `{classTitle}`                                                   | `session {sessionDateTime} was canceled · {roleContext}`                   | `{classTitle} session {sessionDateTime} was canceled`                         | `Reason: {canceledReason}`                    | `Open class` / `outline`    | Push + email, immediate |
| Single class reminder             | `session.reminder.sent`         | `session.reminder.sent`         | `classes` | `normal`    | `Bell` / `info`               | `{classTitle}`                                                   | `starts soon · {roleContext} · Starts at {sessionStartTime}`               | `{classTitle} is scheduled for {sessionDateTime}`                             | Join details, class outline, or session notes | `Open class` / `outline`    | Push, immediate         |
| Single class feedback request     | `session.feedback_request.sent` | `session.feedback_request.sent` | `classes` | `normal`    | `MessageSquareHeart` / `info` | `Share feedback for {classTitle}`                                | `{roleContext} · Your feedback helps improve future sessions`              | `Tell us how the session went`                                                | Feedback form or quick rating UI              | `Give feedback` / `outline` | Push + email, standard  |

## Source Creation Matrix

| Source                        | Created activity event(s)                                                                                                                                                                                 | Dedupe                                                                                                                                    |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Message insert source job     | `message.posted` for channel/class messages and DMs; `message.mentioned` for mentions; `message.thread_reply.posted` for thread replies; `file.uploaded`, `image.uploaded`, or `audio.uploaded` for files | `{eventType}:{messageId}`, `message.mention:{messageId}:{recipientProfileId}`, or `message.thread-reply:{messageId}:{recipientProfileId}` |
| Reaction insert source job    | `reaction.added`                                                                                                                                                                                          | No explicit event dedupe key; source job is `reaction:{reactionId}`                                                                       |
| Session cancel source job     | `class.session.canceled`                                                                                                                                                                                  | `class.session.canceled:{orgId}:{exceptionId}`                                                                                            |
| Session reschedule source job | `class.session.rescheduled`                                                                                                                                                                               | `class.session.rescheduled:{orgId}:{overrideId}`                                                                                          |
| Reminder dispatch job         | `session.reminder.sent` or `session.feedback_request.sent`                                                                                                                                                | `{reminderJob.dedupeKey}:activity`                                                                                                        |
