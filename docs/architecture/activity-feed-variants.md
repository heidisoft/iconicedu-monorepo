# Activity Feed Variants

This reference lists the activity event variants we currently create and how
they render into feed items.

Canonical rendering is owned by
`apps/api/src/lib/activity-feed/definitions/activity-definitions.ts`. Web and
mobile should consume API-projected feed items rather than maintaining local
activity definition mirrors.

## Shared Render Rules

| Field                       | Rule                                                                                                                                                                                                                                                                                     |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `secondary` / context label | Prefer the per-recipient role-aware class label from `activityContext`; otherwise use `learningSpaceTitle`, `channelTopic`, or `title`.                                                                                                                                                  |
| Guardian context            | `{classTitle} for {viewerStudentNames or studentNames} with {teacherNames}`.                                                                                                                                                                                                             |
| Educator context            | `{classTitle} with {studentNames}`.                                                                                                                                                                                                                                                      |
| Child context               | `{classTitle} with {teacherNames}`.                                                                                                                                                                                                                                                      |
| Admin/staff context         | `{classTitle}: {students}, parents {guardians}, teacher {teachers}`.                                                                                                                                                                                                                     |
| Source href                 | Explicit `payload.href`; otherwise `/{orgSlug}/dm/{channelId}`, `/{orgSlug}/c/{channelId}`, or `/{orgSlug}/s/{channelId}` based on route kind/class scope.                                                                                                                               |
| Feed preview                | `summary` renders as the preview card. `expandedContent` renders behind read-more behavior.                                                                                                                                                                                              |
| Icons                       | Feed rows use `content.leading.iconKey` when present, then fall back from `verb`. Web maps icon keys to Lucide icons. Mobile currently maps `CalendarCheck` and `CalendarX` through `Bell` in `ACTIVITY_ICON_MAP`, even though the activity payload still carries the calendar icon key. |

## Activity Table

| Variation                         | Event type                      | Projected verb                  | Tab       | Importance  | Icon key / tone          | Headline primary                                                                                                 | Headline secondary                               | Summary / preview                                                                                   | Expanded content                                                            | Action button               | Default notification              |
| --------------------------------- | ------------------------------- | ------------------------------- | --------- | ----------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | --------------------------- | --------------------------------- |
| Channel or class text message     | `message.posted`                | `message.posted`                | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} sent you a message in`                                                                             | Context label when available                     | `Context: {contextLabel}` when available                                                            | `payload.content`                                                           | `Open messages` / `outline` | Push, standard                    |
| Direct message                    | `message.posted`                | `message.posted`                | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} sent you a message in` when a DM topic/context exists; otherwise `{senderName} sent you a message` | Context label or DM channel topic when available | `Context: {contextLabel}` when available                                                            | `payload.content`                                                           | `Open messages` / `outline` | Push, standard                    |
| Mention                           | `message.mentioned`             | `message.mentioned`             | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} mentioned you in`                                                                                  | Context label when available                     | `Context: {contextLabel}` when available                                                            | `payload.content`                                                           | `Open messages` / `outline` | Push, standard                    |
| Thread reply                      | `message.thread_reply.posted`   | `message.thread_reply.posted`   | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} sent you a message in`                                                                             | Context label when available                     | `Context: {contextLabel}` when available                                                            | `payload.content`                                                           | `Open messages` / `outline` | Push, standard                    |
| File upload                       | `file.uploaded`                 | `file.uploaded`                 | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} sent you a file in`                                                                                | Context label when available                     | `Context: {contextLabel}` when available                                                            | `payload.content` or file name                                              | `Open messages` / `outline` | Push, standard                    |
| Image upload                      | `image.uploaded`                | `image.uploaded`                | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} sent you an image in`                                                                              | Context label when available                     | `Context: {contextLabel}` when available                                                            | `payload.content` or file name                                              | `Open messages` / `outline` | Push, standard                    |
| Voice/audio upload                | `audio.uploaded`                | `audio.uploaded`                | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} sent you a voice message in`                                                                       | Context label when available                     | `Context: {contextLabel}` when available                                                            | `payload.content` or file name                                              | `Open messages` / `outline` | Push, standard                    |
| Message reaction in class/channel | `reaction.added`                | `reaction.added`                | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} reacted {emoji} to your message in`                                                                | Context label when available                     | `Context: {contextLabel}` when available                                                            | None                                                                        | `Open` / `outline`          | Push, standard                    |
| Direct-message reaction           | `reaction.added`                | `reaction.added`                | `all`     | `normal`    | `MessageSquare` / `info` | `{senderName} reacted {emoji} to your direct message`                                                            | Context label when available                     | `Context: {contextLabel}` when available                                                            | None                                                                        | `Open` / `outline`          | Push, standard                    |
| Single class session rescheduled  | `class.session.rescheduled`     | `class.session.rescheduled`     | `classes` | `important` | `CalendarCheck` / `info` | `Class session rescheduled`                                                                                      | Context label or class title                     | `{contextLabel} {payload.summary} Reason: {rescheduledReason}.`; fallback `Class schedule updated.` | None                                                                        | `Open class` / `outline`    | Push + email, immediate, critical |
| Single class session canceled     | `class.session.canceled`        | `class.session.canceled`        | `classes` | `important` | `CalendarX` / `warning`  | `Class session canceled`                                                                                         | Context label or class title                     | `{contextLabel} {payload.summary} Reason: {canceledReason}.`; fallback `Class schedule updated.`    | None                                                                        | `Open class` / `outline`    | Push + email, immediate, critical |
| Single class reminder             | `session.reminder.sent`         | `session.reminder.sent`         | `classes` | `normal`    | `Bell` / `info`          | `Class reminder`                                                                                                 | Context label or class title                     | `{contextLabel} {payload.summary}`; fallback `payload.occurrenceStart`                              | None                                                                        | `Open class` / `default`    | Push + email, immediate, critical |
| Single class feedback request     | `session.feedback_request.sent` | `session.feedback_request.sent` | `classes` | `normal`    | `Bell` / `info`          | `Class feedback requested`                                                                                       | Context label or class title                     | `{contextLabel} {payload.summary or "Share feedback about this session."}`                          | Mobile may render the feedback request UI when `feedbackUiEnabled` is true. | `Open class` / `outline`    | Push + email, immediate           |

## Source Creation Matrix

| Source                                   | Created activity event(s)                                                                                                                                                                                 | Dedupe                                                                                                                                    |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Message insert source job                | `message.posted` for channel/class messages and DMs; `message.mentioned` for mentions; `message.thread_reply.posted` for thread replies; `file.uploaded`, `image.uploaded`, or `audio.uploaded` for files | `{eventType}:{messageId}`, `message.mention:{messageId}:{recipientProfileId}`, or `message.thread-reply:{messageId}:{recipientProfileId}` |
| Reaction insert source job               | `reaction.added`                                                                                                                                                                                          | No explicit event dedupe key; source job is `reaction:{reactionId}`                                                                       |
| Schedule recurrence exception source job | `class.session.canceled`                                                                                                                                                                                  | `session.canceled:{exceptionId}`                                                                                                          |
| Schedule recurrence override source job  | `class.session.rescheduled`                                                                                                                                                                               | `session.rescheduled:{overrideId}`                                                                                                        |
| Due reminder job                         | `session.reminder.sent` or `session.feedback_request.sent`                                                                                                                                                | `{reminderJob.dedupe_key}:activity`                                                                                                       |
