# Codebase Concerns

## Core Sections (Required)

### 1) Top Risks (Prioritized)

| Severity | Concern                                                                                                                                                                                | Evidence                                                                                                                                     | Impact                                                                                                                                                                               | Suggested action                                                                                                                                                |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| High     | Global `ValidationPipe` is registered in `apps/api/src/main.ts` but **zero `class-validator` decorators exist anywhere in `apps/api/src`** — validation is hand-rolled per-DTO instead | `apps/api/src/main.ts`, `apps/api/src/modules/schedules/dto/replace-schedules.dto.ts` (manual `asRequiredString`/`parseParticipant` parsers) | The declared validation strategy doesn't match reality; new endpoints may assume the global pipe validates input when it does not, risking unvalidated input reaching business logic | Either adopt `class-validator` decorators repo-wide, or remove the pipe/dependency and document the hand-rolled pattern as canonical                            |
| Medium   | Mixed data-access pattern inside `apps/api`: some services use `PrismaService`, others call the Supabase client directly                                                               | `apps/api/src/modules/channels/channels.service.ts` (Prisma) vs. `apps/api/src/modules/messages/*` (Supabase client)                         | Two authorization/query code paths to review for security bugs instead of one; harder to reason about consistently                                                                   | `[ASK USER]` confirm whether this is intentional (e.g. Prisma for simple CRUD, Supabase client where RLS session-scoping matters) or migration debt to converge |
| Medium   | `apps/api/package.json` defines `test:e2e` (`./test/jest-e2e.json`) but `apps/api/test/` does not exist                                                                                | `apps/api/package.json`                                                                                                                      | API integration/e2e testing is effectively absent despite being "configured"                                                                                                         | Either implement the e2e suite or remove the dead script reference                                                                                              |
| Low      | Email notification provider is a no-op stub                                                                                                                                            | `apps/api/src/lib/notifications/providers/email-provider.ts` (`sendEmailNotification` is an empty async function)                            | Any feature assuming email delivery works will silently fail with no error/log                                                                                                       | `[ASK USER]` confirm whether email notifications are planned/deprioritized, or should raise a visible error until implemented                                   |
| Low      | `apps/web` and `apps/mobile` HTTP clients have no retry/timeout policy                                                                                                                 | `apps/web/lib/api/http-client.ts`, `apps/mobile/src/lib/api/http-client.ts`                                                                  | Transient API blips surface directly as user-facing errors                                                                                                                           | Consider a bounded retry for idempotent GETs                                                                                                                    |

### 2) Technical Debt

| Debt item                                                                                                                                                                                        | Why it exists                                                                                                             | Where                                                                                                                                         | Risk if ignored                                                                                       | Suggested fix                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Dual web data-fetching pattern: some Server Components use `createApiClient`, others query Supabase directly                                                                                     | ADR-004 (API-first boundary) explicitly calls existing direct table access "migration debt, not a precedent for new work" | `apps/web/lib/org/builders/org.builder.ts` (direct Supabase) vs. `apps/web/lib/dashboard/home-infographic-metrics.ts` (via `createApiClient`) | New contributors may copy the wrong (legacy) pattern; inconsistent authorization enforcement          | Track remaining direct-Supabase call sites and migrate incrementally per ADR-004           |
| `@tanstack/react-query` is a production dependency in `apps/web/package.json` but has zero usage (`useQuery`/`useMutation`/`QueryClient` all return no matches in `app/`, `components/`, `lib/`) | Likely added for a planned feature or copied from mobile's pattern                                                        | `apps/web/package.json`                                                                                                                       | Dead dependency inflates bundle/install size and signals intent that doesn't match code               | `[ASK USER]` confirm whether React Query is planned for web soon, or remove the dependency |
| `packages/shared-types/src/{domain,mappers,projections,validators}` exist as empty scaffold directories (`.gitkeep` only)                                                                        | Scaffolding for a planned reorganization that hasn't landed                                                               | `packages/shared-types/src/`                                                                                                                  | Contributors may not know whether to use these dirs or the existing `rows/vm/payloads/shared/` layout | `[ASK USER]` confirm whether these are active WIP or should be removed until needed        |
| `event_pipeline_jobs` architecture (outbox + poll every minute via pg_cron) has an inherent latency floor                                                                                        | Documented tradeoff in `docs/architecture/event-pipeline-scalability.md`                                                  | `supabase/functions/events-dispatch`, `reminders-dispatch`                                                                                    | Notifications/reminders can lag up to ~1 minute by design                                             | Already documented; no action needed unless latency requirements tighten                   |

### 3) Security Concerns

| Risk                                                                                                                                | OWASP category                                 | Evidence                                                                                                                                                                | Current mitigation                                                                    | Gap                                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| API input validation relies on hand-rolled per-DTO checks, not a uniform validated pipeline                                         | A03 (Injection) / A04 (Insecure Design)        | `apps/api/src/main.ts` (inert `ValidationPipe`), `apps/api/src/modules/schedules/dto/`                                                                                  | Each DTO throws `BadRequestException` on bad input manually                           | No repo-wide guarantee every new endpoint validates input consistently; easy to miss a field                                                                                                                 |
| Mixed Prisma / Supabase-service-client access inside the API                                                                        | A01 (Broken Access Control)                    | `apps/api/src/modules/channels/channels.service.ts` vs. `apps/api/src/modules/messages/*`                                                                               | RLS remains enabled as defense-in-depth per `docs/architecture/database.md`           | Two code paths for authorization logic to audit instead of one                                                                                                                                               |
| No dependency/secret-scanning workflow found — no `.github/dependabot.yml`, no Snyk config, no `.github/workflows/` step for either | A06 (Vulnerable Components)                    | `find .github -iname "dependabot*"` returns nothing; `.github/workflows/` contains only `ci.yml`, `eas-preview-build.yml`, `eas-update.yml`, `native-preview-build.yml` | `SECURITY.md` documents a private vulnerability disclosure process (manual reporting) | No automated dependency-vulnerability alerting confirmed in-repo; `[ASK USER]` confirm whether GitHub's native Dependabot alerts are enabled at the org/repo settings level (not visible from tracked files) |
| No crash-reporting/Sentry integration on mobile or API                                                                              | A09 (Security Logging and Monitoring Failures) | grep for `@sentry/*` across repo — no hits                                                                                                                              | PostHog captures `'api request failed'` events server-side                            | Client-side (mobile) crashes/errors have no dedicated reporting channel beyond PostHog analytics events                                                                                                      |

Hardcoded-secret grep (`sk_(live|test)_`, `AIza`, PEM headers) across tracked `apps/`, `packages/`, `supabase/` returned **no matches** — no evidence of committed secrets found.

### 4) Performance and Scaling Concerns

| Concern                                                                       | Evidence                                                                                                                                                                                                                                                                                                      | Current symptom                                                               | Scaling risk                                                                                             | Suggested improvement                                                                                                      |
| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Event pipeline polls every minute via pg_cron rather than push-based dispatch | `docs/architecture/database.md`, `supabase/functions/events-dispatch`                                                                                                                                                                                                                                         | Up to ~1 minute latency for activity/notification/reminder delivery by design | Acceptable at current scale; may need a push-based path if near-real-time delivery becomes a requirement | Already tracked in `docs/architecture/event-pipeline-scalability.md` — no new action identified                            |
| Several very large single-purpose files                                       | `apps/mobile/src/components/messages/message-item.tsx` (3066 lines), `apps/web/app/(app)/[orgSlug]/sidebar-shell.tsx` (2624), `apps/web/app/(marketing)/seo.ts` (2277), `apps/mobile/src/components/messages/themes/feed-message-list.tsx` (2131), `apps/api/src/modules/messages/messages.service.ts` (2062) | Large files increase re-render/bundle-analysis cost and review difficulty     | Growing further increases risk of merge conflicts and regressions in high-traffic messaging code         | `[ASK USER]` whether decomposing `message-item.tsx`/`messages.service.ts` is planned; both are also high-churn (see below) |

### 5) Fragile/High-Churn Areas

| Area                                                                              | Why fragile                                                                                                 | Churn signal                                                                                 | Safe change strategy                                                                                                                                                                               |
| --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/ci.yml`                                                        | CI pipeline itself under active iteration                                                                   | 14 changes in last 90 days (highest churn in repo)                                           | Small, isolated PRs; validate against a real PR before merging pipeline changes                                                                                                                    |
| `packages/ui-web/src/index.ts`                                                    | Central barrel export for the shared web UI package                                                         | 14 changes in 90 days                                                                        | Additive changes only where possible; check all consumers before removing an export                                                                                                                |
| `apps/web/vitest.config.ts`                                                       | Test config touched frequently alongside feature work                                                       | 13 changes in 90 days                                                                        | Keep config changes in dedicated commits, separate from feature PRs                                                                                                                                |
| `apps/web/flags.ts` + `apps/web/flags.test.ts`                                    | Every new gated feature touches this file (by design — `scripts/check-feature-flag-gating.mjs` enforces it) | 12 + 10 changes in 90 days                                                                   | Expected churn given the flag-gating convention; not itself a red flag                                                                                                                             |
| `apps/mobile/app/(auth)/login.tsx`, `apps/mobile/src/providers/auth-provider.tsx` | Auth flow (OTP + OAuth) is complex and security-sensitive                                                   | 11 and 7 changes in 90 days respectively                                                     | Extra scrutiny on auth-flow PRs; `auth-provider.tsx` explicitly works around an "RLS race condition" (`setSession` called before `checkOrgAssignment`) — any reordering here needs careful testing |
| `apps/api/src/modules/channels/channels.service.ts`                               | Core messaging data-access service                                                                          | 9 changes in 90 days                                                                         | Pair with messaging-domain tests before merging                                                                                                                                                    |
| `apps/mobile/src/components/messages/message-item.tsx` (3066 lines)               | Largest file in the mobile app, core message rendering                                                      | Appears in largest-files list; not in the top churn-20 but size alone raises regression risk | Favor targeted edits over broad refactors without dedicated test coverage first                                                                                                                    |

### 6) `[ASK USER]` Questions

1. [ASK USER] Is the mixed Prisma/Supabase-client data-access pattern inside `apps/api` (§1, §3) an intentional split (e.g. Prisma for simple CRUD, Supabase session client where RLS scoping is required) or unconverged migration debt that should be standardized on one approach?
2. [ASK USER] Is the inert global `ValidationPipe` (no `class-validator` decorators anywhere) a known gap, or is DTO validation intentionally hand-rolled per-endpoint going forward? Should the `class-validator`/`class-transformer` dependencies and pipe be removed if unused?
3. [ASK USER] Is `@tanstack/react-query` in `apps/web` a planned-but-unstarted adoption, or should it be removed as an unused dependency?
4. [ASK USER] Are the empty `packages/shared-types/src/{domain,mappers,projections,validators}` scaffold directories active work-in-progress, or leftover scaffolding that should be removed until needed?
5. [ASK USER] Is the `email-provider.ts` no-op stub intentional (email notifications deprioritized), or a gap that should raise a visible error/alert instead of silently succeeding?
6. [ASK USER] Is `apps/api`'s configured-but-empty `test:e2e` (missing `apps/api/test/` directory) tracked as planned work, or should the script be removed until implemented?
7. [ASK USER] Is GitHub's native Dependabot (or an equivalent dependency-vulnerability scanner) enabled at the repo/org settings level? No `dependabot.yml` or scanning step is visible in tracked files, so this can't be confirmed from the repository alone.

### 7) Evidence

- `docs/codebase/.codebase-scan.txt` (high-churn files, TODO/FIXME scan, code metrics)
- `apps/api/src/main.ts`, `apps/api/src/modules/schedules/dto/replace-schedules.dto.ts`
- `apps/api/src/lib/notifications/providers/email-provider.ts`
- `apps/web/package.json` (unused `@tanstack/react-query`)
- `packages/shared-types/src/` (empty scaffold dirs)
- `docs/decisions/004-api-first-frontend-boundary.md`, `docs/architecture/event-pipeline-scalability.md`

## Extended Sections (Optional)

Not populated — this pass focused on evidence-backed, actionable concerns rather than a full bug inventory or cost/effort estimates.
